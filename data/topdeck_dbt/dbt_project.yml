name: 'topdeck_dbt'
version: '1.0.0'

profile: 'topdeck'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  topdeck_dbt:
    staging:
      +materialized: view
      +schema: staging
    core:
      +materialized: table
      +schema: core

seeds:
  topdeck_dbt:
    +schema: core
